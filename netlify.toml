[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_NETLIFY_EDGE_PROXY = "true"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
